{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Ship Module Definitions",
  "description": "Components that can be installed on a ship. Each module has a category that determines its fields.",
  "type": "object",
  "additionalProperties": {
    "discriminator": {
      "propertyName": "category"
    },
    "oneOf": [
      {
        "type": "object",
        "description": "Command bridge. Mandatory, one per ship.",
        "required": ["id", "nameKey", "descriptionKey", "category"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "Unique identifier." },
          "nameKey": {
            "type": "string",
            "description": "i18n key for the display name."
          },
          "descriptionKey": {
            "type": "string",
            "description": "i18n key for the description."
          },
          "category": { "const": "bridge" }
        }
      },
      {
        "type": "object",
        "description": "Ship reactor. Mandatory, one per ship.",
        "required": ["id", "nameKey", "descriptionKey", "category"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "Unique identifier." },
          "nameKey": {
            "type": "string",
            "description": "i18n key for the display name."
          },
          "descriptionKey": {
            "type": "string",
            "description": "i18n key for the description."
          },
          "category": { "const": "reactor" }
        }
      },
      {
        "type": "object",
        "description": "Propulsion engine.",
        "required": [
          "id",
          "nameKey",
          "descriptionKey",
          "category",
          "speed",
          "fuelEfficiency"
        ],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "Unique identifier." },
          "nameKey": {
            "type": "string",
            "description": "i18n key for the display name."
          },
          "descriptionKey": {
            "type": "string",
            "description": "i18n key for the description."
          },
          "category": { "const": "engine" },
          "speed": {
            "type": "number",
            "description": "Ship speed.",
            "exclusiveMinimum": 0
          },
          "fuelEfficiency": {
            "type": "number",
            "description": "Fuel efficiency rating.",
            "exclusiveMinimum": 0
          }
        }
      },
      {
        "type": "object",
        "description": "Fuel storage tank.",
        "required": ["id", "nameKey", "descriptionKey", "category", "capacity"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "Unique identifier." },
          "nameKey": {
            "type": "string",
            "description": "i18n key for the display name."
          },
          "descriptionKey": {
            "type": "string",
            "description": "i18n key for the description."
          },
          "category": { "const": "fuelTank" },
          "capacity": {
            "type": "number",
            "description": "Fuel capacity in tonnes.",
            "exclusiveMinimum": 0
          }
        }
      },
      {
        "type": "object",
        "description": "Crew quarters. Houses one crew member.",
        "required": ["id", "nameKey", "descriptionKey", "category"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "Unique identifier." },
          "nameKey": {
            "type": "string",
            "description": "i18n key for the display name."
          },
          "descriptionKey": {
            "type": "string",
            "description": "i18n key for the description."
          },
          "category": { "const": "crewQuarters" }
        }
      },
      {
        "type": "object",
        "description": "Cargo hold for mined resources.",
        "required": ["id", "nameKey", "descriptionKey", "category", "capacity"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "Unique identifier." },
          "nameKey": {
            "type": "string",
            "description": "i18n key for the display name."
          },
          "descriptionKey": {
            "type": "string",
            "description": "i18n key for the description."
          },
          "category": { "const": "cargoBay" },
          "capacity": {
            "type": "number",
            "description": "Cargo capacity in tonnes.",
            "exclusiveMinimum": 0
          }
        }
      },
      {
        "type": "object",
        "description": "Ship-mounted spectral scanner for asteroid discovery.",
        "required": [
          "id",
          "nameKey",
          "descriptionKey",
          "category",
          "sensitivity"
        ],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "Unique identifier." },
          "nameKey": {
            "type": "string",
            "description": "i18n key for the display name."
          },
          "descriptionKey": {
            "type": "string",
            "description": "i18n key for the description."
          },
          "category": { "const": "scanner" },
          "sensitivity": {
            "type": "number",
            "description": "Scanner sensitivity for detection formula.",
            "exclusiveMinimum": 0
          }
        }
      },
      {
        "type": "object",
        "description": "Bay that holds one deployable machine.",
        "required": ["id", "nameKey", "descriptionKey", "category"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "Unique identifier." },
          "nameKey": {
            "type": "string",
            "description": "i18n key for the display name."
          },
          "descriptionKey": {
            "type": "string",
            "description": "i18n key for the description."
          },
          "category": { "const": "machineBay" }
        }
      }
    ]
  }
}
