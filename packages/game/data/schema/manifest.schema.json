{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Data Pack Manifest",
  "description": "Manifest format for Asteroid Miner data packs. The base game, DLCs, and community mods all use this format. Place a manifest.json at the root of your data pack folder.",
  "type": "object",
  "required": [
    "id",
    "nameKey",
    "version",
    "type",
    "descriptionKey",
    "author",
    "gameVersion"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path or URL to the JSON Schema for this file."
    },
    "id": {
      "type": "string",
      "description": "Unique identifier for this data pack. Must be unique across all loaded packs.",
      "examples": ["base", "deep_core", "better_drills"]
    },
    "nameKey": {
      "type": "string",
      "description": "i18n key for the display name."
    },
    "version": {
      "type": "string",
      "description": "Version of this data pack in semver format (MAJOR.MINOR.PATCH). Bump this when you release updates so the game can detect changes.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "examples": ["0.1.0", "1.0.0", "2.3.1"]
    },
    "type": {
      "type": "string",
      "description": "What kind of data pack this is. \"base\" is the core game data. \"dlc\" is official expansion content. \"mod\" is community-created content.",
      "enum": ["base", "dlc", "mod"]
    },
    "descriptionKey": {
      "type": "string",
      "description": "i18n key for the description."
    },
    "author": {
      "type": "string",
      "description": "Who made this pack."
    },
    "gameVersion": {
      "type": "string",
      "description": "Minimum game version this pack requires.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "examples": ["0.1.0", "1.0.0"]
    },
    "tags": {
      "type": "array",
      "description": "Categories for filtering in the mod browser.",
      "default": [],
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "array",
      "description": "Other data packs this pack depends on or conflicts with. Each entry is a string with the format: \"[prefix] pack_id [operator version]\". No prefix means a hard requirement (the pack won't load without it). Prefix \"?\" means optional (affects load order if the other pack is present, ignored if missing). Prefix \"!\" means incompatible (the game refuses to load both). You can add a version constraint with an operator (>=, <=, >, <, =) followed by a semver version.",
      "default": [],
      "items": {
        "type": "string",
        "pattern": "^(!|\\?)?\\s*[a-z][a-z0-9_.]*(\\s*(>=|<=|>|<|=)\\s*\\d+\\.\\d+\\.\\d+)?$",
        "examples": [
          "base >= 1.0.0",
          "? deep_core",
          "! other_drill_overhaul",
          "? some_mod >= 1.0.0"
        ]
      }
    }
  }
}
